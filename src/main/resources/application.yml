server:

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5433/usersDB
    username: admin
    password: Qwnmzxop10
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

jwt:
  secret: "foodDeliveryPlatformSecretKeyMustBeAtLeast32CharactersLong"
  expiration: 86400000

logging:
  level:
    org.user_service: DEBUG
    org.springframework.security: DEBUG